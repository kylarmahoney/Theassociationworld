[build]
builder = "nixpacks"
buildCommand = "cd ../../ && pnpm install && pnpm --filter @workspace/association-world run build"

[deploy]
startCommand = "cd ../../ && pnpm --filter @workspace/association-world run serve"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
