[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/association-world run build"

[deploy]
startCommand = "pnpm --filter @workspace/association-world run serve"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
